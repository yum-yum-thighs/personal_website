class Player {
    constructor(x = 100, y = 480) {
        this.x = x;
        this.y = y;
        this.speed = 4;
    }

    update(keys) {
        // Player movement logic will go here
    }

    draw(ctx) {
        // Left empty to prevent rendering the green box
    }
}
